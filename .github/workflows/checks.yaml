---
name: checks

"on":
  pull_request:
    branches:
      - main
permissions:
  contents: read
  pull-requests: read

jobs:
  prettier:
    uses: broadinstitute/shared-workflows/.github/workflows/prettier.yaml@v6.2.0
  static-analysis:
    uses: broadinstitute/shared-workflows/.github/workflows/puppet-lint.yaml@v6.2.0
  unit-tests:
    uses: broadinstitute/shared-workflows/.github/workflows/puppet-unit-test.yaml@v6.2.0
