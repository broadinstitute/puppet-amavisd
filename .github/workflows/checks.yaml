---
name: 'checks'

'on':
  pull_request:
    branches:
      - 'main'

jobs:
  static-analysis:
    uses: broadinstitute/shared-workflows/.github/workflows/puppet-lint.yaml@v6.0.0
  unit-tests:
    uses: broadinstitute/shared-workflows/.github/workflows/puppet-unit-test.yaml@v5.0.1
