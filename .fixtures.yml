fixtures:
  forge_modules:
    apt: 'puppetlabs/apt'
    concat:
      repo: 'puppetlabs/concat'
      ref: '2.2.1'
    epel: 'stahnma/epel'
    stdlib:
      repo: 'puppetlabs/stdlib'
      ref: '4.12.0'
  symlinks:
    amavisd: "#{source_dir}"
